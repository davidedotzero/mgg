{
  "compilerOptions": {
    // 🛠️ 1. "compilerOptions": ตั้งค่าการคอมไพล์
    // อธิบาย:
    //     ส่วนนี้ใช้กำหนดพฤติกรรมของ TypeScript Compiler (แม้จะใช้ JavaScript ก็ยังใช้ได้)
    //     ช่วยให้โปรเจกต์รู้จัก Path Alias และพาธไฟล์แบบย่อ
    // เหตุผล:
    //     ทำให้ Import ไฟล์ในโปรเจกต์ง่ายขึ้นและเป็นระเบียบ
    //     ลดการใช้พาธแบบยาวซ้อนลึก เช่น ../../../lib/firebaseConfig
    "baseUrl": ".",
    // 🛠️ 2. "baseUrl": "."
    // อธิบาย:
    //     กำหนด . (root ของโปรเจกต์) เป็นฐานในการ Resolve พาธ
    //     ทำให้ใช้พาธแบบสัมพัทธ์ (Relative) ได้ง่ายขึ้น
    // เหตุผล:
    //     ป้องกันปัญหาเวลาย้ายไฟล์หรือเปลี่ยนโฟลเดอร์
    //     ช่วยให้ Import พาธสั้นและอ่านง่ายขึ้น
    "paths": {
      "@/*": [
        "./*"
      ]
    }
    //     อธิบาย:
    //     สร้าง Path Alias ชื่อ @/ เพื่อแทนที่พาธจาก root
    //     @/*: หมายถึง ไฟล์หรือโฟลเดอร์ใด ๆ ใน root
    //     ./*: หมายถึง ไฟล์หรือโฟลเดอร์ใด ๆ ใน root เช่นกัน
    // เหตุผล:
    //     ทำให้ Import ไฟล์สั้นลง เช่น:
    // // เดิม
    // import { db } from "../../../lib/firebaseConfig";
    // // หลังใช้ Path Alias
    // import { db } from "@/lib/firebaseConfig";
    // ลดความซับซ้อนของพาธซ้อนลึกและทำให้โค้ดอ่านง่ายขึ้น
  }
}